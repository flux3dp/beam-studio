const mockSymbolMaker = {
  createImageSymbol: jest.fn(),
  makeImageSymbol: jest.fn(),
  makeSymbol: jest.fn(),
  reRenderAllImageSymbols: jest.fn(),
  reRenderImageSymbol: jest.fn(),
  reRenderImageSymbolArray: jest.fn(),
  switchImageSymbol: jest.fn(),
  switchImageSymbolForAll: jest.fn(),
};

export default mockSymbolMaker;
