export enum Mode {
  WORKING = 'WORKING',
  PREVIEW = 'PREVIEW',
  FILE = 'FILE',
  FILE_PREVIEW = 'FILE_PREVIEW',
  CAMERA = 'CAMERA',
  CAMERA_RELOCATE = 'CAMERA_RELOCATE',
}

export enum ItemType {
  FILE = 'FILE',
  FOLDER = 'FOLDER',
}

export default {
  Mode,
  ItemType,
};
