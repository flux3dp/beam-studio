import { IChangeLog } from 'interfaces/IChangeLog';

const CHANGES_TW = {
  added: [
  ],
  fixed: [
    '修正旋轉後縮放後位移問題。',
  ],
  changed: [
  ],
};

const CHANGES_EN = {
  added: [
  ],
  fixed: [
    'Fixed position shifting error after rotating and scaling some objects in some files.',
  ],
  changed: [
  ],
};

export default {
  CHANGES_TW,
  CHANGES_EN,
} as IChangeLog;
